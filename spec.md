# Spec: The Machine MVP — 本地监控告警系统

## Problem Statement

监控摄像头正在成为家庭和办公室的标配，但现有方案存在两难困境：
- 商业云方案（如萤石、小米）将视频数据上传到云端，存在隐私泄露风险
- 本地方案（如 Blue Iris、Shinobi）功能强大但配置复杂，缺乏智能告警
- 没有方案能在**本地运行 + 实时 AI 检测 + 即时消息告警**这三者间取得平衡

需要一个轻量级本地系统：接入现有 RTSP 摄像头，用 AI 实时分析画面，
仅在检测到异常时通过即时消息推送告警。

## Success Metrics

1. 从摄像头帧到达 → QQ 告警推送的端到端延迟 < 5 秒
2. 正常活动模式下误报率 < 1 次/天
3. 摄像头断流后 30 秒内自动恢复，服务不崩溃
4. 系统连续运行 72 小时无内存泄漏
5. CPU 推测下单帧处理 < 2 秒

## User Stories

1. 作为 Admin，我接入 RTSP 摄像头后即可在 QQ 上收到实时告警
2. 作为 Admin，我可以查看告警详情（截图 + 推理原因）
3. 作为 Admin，我能在 QQ 上查询系统状态："当前状态"
4. 作为 Admin，我可以将已知人员录入白名单，避免误报
5. 作为 Admin，我可以设置免打扰时段，夜间不被打扰

## Acceptance Criteria

### A1 摄像头感知
- [ ] 支持通过 RTSP URL 接入摄像头
- [ ] 以可配置的间隔（默认 2 秒）拉取帧
- [ ] 断流时自动重连（最多 3 次），服务不崩溃
- [ ] 每帧携带 camera_id、时间戳、帧数据

### A2 目标检测
- [ ] 检测画面中的人
- [ ] 支持区分已知人脸（白名单）与未知人脸
- [ ] 检测结果包含类别、置信度、位置信息
- [ ] CPU 下单帧检测 < 2 秒

### A3 异常判定
- [ ] 出现未知人脸 → 触发告警
- [ ] 非活动时段有移动目标 → 触发告警
- [ ] 同区域人员滞留超过阈值 → 触发告警
- [ ] 同类告警冷却期内不重复推送（默认 5 分钟）

### A4 告警推送
- [ ] 告警以 `🚨 Number #{ID}` 格式推送
- [ ] 告警附带时间、区域、类型、置信度、推理详情
- [ ] 告警附带截图证据
- [ ] 支持静默模式 / 免打扰时段
- [ ] 消息长度 < 2000 字符

### A5 配置管理
- [ ] 摄像头、白名单、阈值均可配置
- [ ] 配置变更热重载，无需重启
- [ ] 白名单支持增删改

## Non-Goals

- 不实现语义搜索（本期仅规则匹配 + 评分）
- 不支持 PTZ 摄像头控制
- 不实现视频录制 / NVR 功能（仅帧检测 + 截图证据）
- 不实现 Web 管理界面（本期仅 QQ 交互）
- 不实现音频分析
- 不支持跨摄像头追踪同一目标
- 不实现云端同步/备份

## Constraints

- CPU 推理优先（GPU 可选加速）
- Python 3.10+
- 存储使用 SQLite（零配置）
- 通知通道为 QQ Bot（通过 OpenClaw）
- FFmpeg 用于拉取 RTSP 流
