# Project Constitution — The Machine

> 不可变约束。所有 Spec 都必须遵守。

## 1. 隐私与安全

- 所有视频帧、检测结果**仅在本地处理**，禁止上传任何数据到云端 API
- 人脸特征数据本地存储，禁止通过网络传输
- 截留的证据（截图/视频片段）仅保留 7 天自动清理
- 每个摄像头支持独立暂停/静音，Admin 可随时关闭任意传感器

## 2. 系统设计

- 所有检测与告警链路必须支持**离线运行**，不依赖外网
- 告警系统支持冷却机制，同一类型告警在冷却期内不重复推送
- 摄像头断流必须自动重连，最多重试 3 次，服务不崩溃
- 配置变更支持热重载，无需重启服务

## 3. 代码质量

- 所有模块必须提供类型注解
- 错误处理使用自定义异常层级，禁止吞异常
- 所有外部依赖（FFmpeg 进程等）必须支持优雅关闭

## 4. 可解释性

- 每次告警必须附带推理依据（为什么触发的、什么规则匹配的）
- 系统状态支持随时查询：运行时间、活跃摄像头数、事件统计
- 告警事件永久记录到本地数据库（SQLite）

## 5. 兼容性

- 支持 CPU 推理（GPU 可选加速）
- 无云依赖，不绑定任何云服务商
- 配置使用 JSON 格式，支持手动编辑
